[project]
name = "ble-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "bleak>=1.1.1",
]

[tool.setuptools]
py-modules = ["main", "stay_connected"]